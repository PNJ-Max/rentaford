import { ScrollProgress } from './scroll-progress';

describe('ScrollProgress', () => {
  it('should create an instance', () => {
    const directive = new ScrollProgress();
    expect(directive).toBeTruthy();
  });
});
